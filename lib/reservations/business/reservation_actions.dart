class ReservationActions{

  void extractFromFile(){
    //extract reservations from file
  }

  void listReservationFiles(){
    //list all the local reservation files
    // files stored inside "documents/reservations"
    //
  }

  void readReservationFile(){

    //attempt to read the reservation file
    //throws an exception ( UnsupportedFileException ) if it cannot detect wether it's from Airbnb or Booking.
    //else it detects wether it's from booking or Airbnb and procceeds with the right extractor

    //Returns a List<Reservation>
  }


}